org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Optional: Web Client ID from Google Cloud (same as GOOGLE_CLIENT_ID for the web app).
# Required for "Sign in with Google" in the app. Get it from APIs & Credentials > OAuth 2.0 Client IDs > Web client.
# GOOGLE_WEB_CLIENT_ID=123456789-xxx.apps.googleusercontent.com
